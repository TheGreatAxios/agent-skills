interface:
  display_name: "Zod"
  short_description: "TypeScript-first schema validation"
  brand_color: "#3B82F6"
  default_prompt: "Create a Zod schema for"

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "zodDocs"
      description: "Zod documentation MCP server"
      transport: "streamable_http"
      url: "https://zod.dev/mcp"
